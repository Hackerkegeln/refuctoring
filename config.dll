(!(
        !cmp3.setNumbre(num).compare222() || !c5.setNumbre(num).compare222()
      ) ||
        cmp3.setNumbre(comparatоr3).compare222()) &&
      isNumber(
        +(
          !(
            !cmp3.setNumbre(num).compare222() || !c5.setNumbre(num).compare222()
          ) || cmp3.setNumbre(comparatоr3).compare222()
        )
      )